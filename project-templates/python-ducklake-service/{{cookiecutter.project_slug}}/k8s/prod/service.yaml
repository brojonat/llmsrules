apiVersion: v1
kind: Service
metadata:
  name: {{cookiecutter.project_slug}}
  namespace: {{cookiecutter.k8s_namespace}}
spec:
  selector:
    app: {{cookiecutter.project_slug}}
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
